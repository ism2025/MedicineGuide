<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.basmatech.medicineguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListActivities.AccordingToGroupActivity" />
        <activity android:name=".ListActivities.AccordingToDepartmentActivity" />
        <activity android:name=".ListActivities.MedicinesListActivity" />
        <activity android:name=".Activities.MedicineActivity" />
        <activity android:name=".Activities.DoseActivity"></activity>
    </application>

</manifest>